name: repository_size
run-name: "Giving me the size"
on: [push]
jobs:
  size:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo "${GITHUB_REF#refs/heads/}"
      - run: du -skh --exclude=".git"